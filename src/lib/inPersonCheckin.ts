/**
 * Admin-only physical presence (who came in person vs joined online) — not a
 * programme session. Stored on `attendance/{uid}` next to `session-*` fields.
 */
export const IN_PERSON_MAY23_2026_FIELD = "inPersonMay23_2026" as const;
